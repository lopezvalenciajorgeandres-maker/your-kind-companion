# Copia este archivo como `.env.local` (ese archivo NO se sube a GitHub) y pega tu clave secreta.
#
# Dónde encontrarla: https://supabase.com/dashboard/project/tjiybeymcukkauoyfosd/settings/api-keys
#   -> "Secret keys" (sb_secret_...) o la "service_role" heredada (JWT largo).
#
# La usan solo las funciones del servidor para: crear el negocio en el onboarding
# y registrar reservas desde la página pública de agendamiento.
SUPABASE_SERVICE_ROLE_KEY=""
